{
  "name": "SPA content collaboration app",
  "version": "1.0.0",
  "description": "A fullstack single page app with just javascript express.js for **CRUD** requests and **MongoDB** for database\r basically the MERN stack without the R",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\", && exit 1",
    "dev": "node --env-file=.env --watch ./server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "mongoose": "^9.1.3",
    "multer": "^2.0.2"
  }
}
